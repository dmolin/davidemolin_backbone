var homeIndex = {
    views: require('./views'),
    deps: {
        common: require('../common'),
        carousel: require('../carousel'),
        projects: require('../projects')
    }
};

module.exports = homeIndex;
